// Loaded lazily by <LazyMotion> so motion's feature bundle (layout animations
// included) lands in its own chunk instead of the main one.
import { domMax } from 'motion/react'

export default domMax
